name: superuser
description: A product by shanmukeshwar

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons:
  firebase_auth:
  firebase_core:
  cloud_firestore:
  material_design_icons_flutter:
  permission_handler:
  multi_image_picker:
  flutter_phoenix:
  firebase_storage:
  date_time_format:
  image:
  cached_network_image:
  photo_view:
  flutter_spinkit:
  lazy_load_scrollview:
  provider:
  flutter_data_stream_builder:
  get:
  url_launcher:
  cloud_functions:
  image_picker:
  image_cropper:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
